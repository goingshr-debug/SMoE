# SMoEaligned — Python package requirements
# Tested with Python 3.13.2 (cp313t free-threading build) in conda env "Nmoe"
#
# Quick install:
#   conda create -n Nmoe python=3.13 -c conda-forge
#   conda activate Nmoe
#   pip install -r requirements.txt
#
# For CUDA support (recommended), install PyTorch with your CUDA version first:
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu121
# Then run:
#   pip install -r requirements.txt --ignore-installed torch

# ── Core deep learning ────────────────────────────────────────────────────────
torch==2.7.1

# ── HuggingFace ecosystem ─────────────────────────────────────────────────────
transformers==4.50.3
tokenizers==0.21.1
huggingface-hub==0.32.4
datasets>=2.0.0
safetensors==0.5.3
accelerate>=0.34.0
datasets>=2.0.0          # required for auto-downloading benchmark datasets

# ── Scientific computing ──────────────────────────────────────────────────────
numpy==2.3.0
scipy==1.15.3

# ── Utilities ─────────────────────────────────────────────────────────────────
psutil==7.0.0
tqdm==4.67.1

# ── Optional: Flash Attention 2 (GPU only, requires CUDA build) ───────────────
# flash-attn>=2.5.0
